# Jenkins Scripts

This directory contains utility scripts for Jenkins setup and management.

## Scripts

- `install-jenkins-plugins.sh` - Installs required Jenkins plugins
- `setup-jenkins.sh` - Initial Jenkins setup script
